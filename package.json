{
  "name": "root",
  "private": true,
  "devDependencies": {
    "eslint": "7.10.0",
    "lerna": "^4.0.0",
    "prettier": "2.3.0"
  },
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build:front": "lerna run build --stream --scope=@monorepo/front"
  },
  "workspaces": [
    "packages/*"
  ]
}
